const express=require("express");const cors=require("cors");const app=express();app.use(cors({origin:"*"}));app.use(express.json());const GROQ_API_KEY=process.env.GROQ_API_KEY;app.post("/api/chat",async(req,res)=>{try{const{messages}=req.body;const r=await fetch("https://api.groq.com/openai/v1/chat/completions",{method:"POST",headers:{"Content-Type":"application/json",Authorization:"Bearer "+GROQ_API_KEY},body:JSON.stringify({model:"llama3-8b-8192",messages:[{role:"system",content:"You are NexusAI - a campus assistant. Help students with placements, academics, career and wellbeing. Use emojis and be encouraging."},...messages]})});const d=await r.json();res.json({reply:d.choices[0].message.content})}catch(e){res.status(500).json({error:"Something went wrong."})}}});app.get("/api/health",(req,res)=>res.json({status:"ok"}));app.listen(process.env.PORT||3001,()=>console.log("Running"));